# React Native / Expo tooling resolves modules through a flat node_modules.
# pnpm's default isolated (symlinked) layout hides nested deps from Metro, so we
# use a hoisted linker across the workspace.
node-linker=hoisted
